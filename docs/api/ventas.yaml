{
  "openapi": "3.0.3",
  "info": {
    "title": "TiendaTech ventas",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "/"
    }
  ],
  "paths": {
    "/api/facturas": {
      "post": {
        "summary": "Crear o procesar /api/facturas",
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Respuesta uniforme",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApiResponse"
                }
              }
            }
          },
          "401": {
            "description": "JWT ausente, inválido o expirado"
          }
        }
      }
    },
    "/api/facturas/{id}": {
      "get": {
        "summary": "Consultar /api/facturas/{id}",
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Respuesta uniforme",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApiResponse"
                }
              }
            }
          },
          "401": {
            "description": "JWT ausente, inválido o expirado"
          }
        }
      }
    },
    "/api/facturas/{id}/detalle": {
      "get": {
        "summary": "Consultar /api/facturas/{id}/detalle",
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Respuesta uniforme",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApiResponse"
                }
              }
            }
          },
          "401": {
            "description": "JWT ausente, inválido o expirado"
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    },
    "schemas": {
      "ApiResponse": {
        "type": "object",
        "required": [
          "status",
          "data",
          "message",
          "timestamp"
        ],
        "properties": {
          "status": {
            "type": "integer"
          },
          "data": {
            "nullable": true
          },
          "message": {
            "type": "string"
          },
          "timestamp": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    }
  }
}

