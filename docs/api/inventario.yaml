{
  "openapi": "3.0.3",
  "info": {
    "title": "TiendaTech inventario",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "/"
    }
  ],
  "paths": {
    "/api/inventario": {
      "get": {
        "summary": "Consultar /api/inventario",
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Respuesta uniforme",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApiResponse"
                }
              }
            }
          },
          "401": {
            "description": "JWT ausente, inválido o expirado"
          }
        }
      }
    },
    "/api/inventario/{productoId}": {
      "get": {
        "summary": "Consultar /api/inventario/{productoId}",
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Respuesta uniforme",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApiResponse"
                }
              }
            }
          },
          "401": {
            "description": "JWT ausente, inválido o expirado"
          }
        }
      }
    },
    "/api/sp/movimiento-inventario": {
      "post": {
        "summary": "Crear o procesar /api/sp/movimiento-inventario",
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Respuesta uniforme",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApiResponse"
                }
              }
            }
          },
          "401": {
            "description": "JWT ausente, inválido o expirado"
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    },
    "schemas": {
      "ApiResponse": {
        "type": "object",
        "required": [
          "status",
          "data",
          "message",
          "timestamp"
        ],
        "properties": {
          "status": {
            "type": "integer"
          },
          "data": {
            "nullable": true
          },
          "message": {
            "type": "string"
          },
          "timestamp": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    }
  }
}

