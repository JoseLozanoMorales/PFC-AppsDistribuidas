spring.application.name=TiendaTechV18
spring.datasource.url=${TT_DATASOURCE_URL}
spring.datasource.username=${TT_DATASOURCE_USERNAME}
spring.datasource.password=${TT_DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.proc.param_null_passing=true
app.upload-root=${APP_UPLOAD_ROOT:${user.dir}/uploads}
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=15MB
spring.jpa.properties.hibernate.default_schema=public
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.orm.jdbc.bind=TRACE

# Todo para validar correo
# SMTP (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${TT_MAIL_USERNAME}
spring.mail.password=${TT_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# OTP (tiempo de usos en OtpService/CacheConfig)
otp.ttl-minutes=10
otp.length=6
otp.resend.cooldown-seconds=60
otp.rate.max-per-15min=3

auth.access.minutes=10
auth.absolute.hours=8
#auth.idle.ADMIN=1
#.idle.TRABAJADOR=10
#auth.idle.CLIENTE=0
auth.cookie.domain=localhost
auth.cookie.secure=false
auth.cookie.samesite=Lax
auth.jwt.secret=${AUTH_JWT_SECRET}


#AHORA PARA LA API DE PAYPAL
paypal.mode=sandbox
paypal.clientId=Acrd3u7YQ4mgh0CkKbk8EIm3nqVnUYitUa4x1Ftoazxg0Zcm6VaheMRw9-2oSo3YK5tkzsJ8zc0piv_t
paypal.clientSecret=EG6AQ1Y4rbUage9XNjyFYEPap20V8VEgRwkWVveuUkytBtSaO5er3eCEDMcTWbvvoFrbgeKo7SbzGcY8
paypal.currency=USD


logging.level.org.springframework.web.servlet.DispatcherServlet=TRACE
logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping=TRACE


# --- Temporizador por rol (minutos)
auth.idle.ADMIN=1
auth.idle.TRABAJADOR=1
auth.idle.CLIENTE=0

# --- Sesión y cookies (mejor en PROD con HTTPS)
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=Lax
server.servlet.session.cookie.secure=${COOKIE_SECURE:false}

# --- Logs (evita TRACE en prod)
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.orm.jdbc.bind=INFO

# URL base del frontend para armar el link a /cambiar-password.html
app.frontend.base-url=http://localhost:8080

# URL base del frontend para armar el link a /cambiar-password.html
app.frontend.base-url=http://localhost:8080