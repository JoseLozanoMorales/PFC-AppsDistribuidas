import { FormEvent, useState } from 'react'
import { Link } from 'react-router-dom'
import { api } from '../services/api'
export default function RecoveryView() { const [correo,setCorreo]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(''),[sent,setSent]=useState(false); const recover=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await api('/api/usuarios/recuperar-password',{method:'POST',body:JSON.stringify({correo})});setSent(true)}catch(cause){setError(cause instanceof Error?cause.message:'No se pudo enviar el correo.')}finally{setBusy(false)}}; return <section className="auth-page"><div className="auth-intro recovery-art"><p className="eyebrow">Recuperación segura</p><h1>Volvamos a poner tu cuenta en marcha.</h1><p>Recibirás una contraseña temporal en tu correo registrado.</p></div><form className="auth-form compact" onSubmit={recover}><p className="eyebrow">Acceso</p><h2>Recuperar contraseña</h2>{!sent?<><p>Ingresa el correo asociado a tu cuenta.</p><label>Correo electrónico<input value={correo} onChange={e=>setCorreo(e.target.value)} type="email" required placeholder="nombre@correo.com" /></label>{error&&<p className="alert">{error}</p>}<button className="button" disabled={busy}>{busy?'Enviando…':'Enviar contraseña temporal'}</button></>:<div className="success-panel"><span>✓</span><h3>Revisa tu correo</h3><p>Si la dirección está registrada, recibirás instrucciones en unos minutos.</p></div>}<p className="auth-switch"><Link to="/login">← Volver al inicio de sesión</Link></p></form></section> }
