services:
  crdb-1:
    image: cockroachdb/cockroach:v23.2.4
    container_name: tiendatech-crdb-1
    restart: unless-stopped
    command:
      - start
      - --insecure
      - --advertise-addr=crdb-1:26257
      - --listen-addr=0.0.0.0:26257
      - --http-addr=0.0.0.0:8080
      - --join=crdb-1:26257,crdb-2:26257,crdb-3:26257
      - --locality=region=aws,zone=z1
      - --cache=64MiB
      - --max-sql-memory=64MiB
    ports:
      - "0.0.0.0:26257:26257"
      - "127.0.0.1:8091:8080"
    volumes:
      - crdb1-data:/cockroach/cockroach-data
    networks: [crdb-net]
    healthcheck:
      test: ["CMD-SHELL", "cockroach sql --insecure --host=localhost:26257 -e 'SELECT 1' >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  crdb-2:
    image: cockroachdb/cockroach:v23.2.4
    container_name: tiendatech-crdb-2
    restart: unless-stopped
    command:
      - start
      - --insecure
      - --advertise-addr=crdb-2:26257
      - --listen-addr=0.0.0.0:26257
      - --http-addr=0.0.0.0:8080
      - --join=crdb-1:26257,crdb-2:26257,crdb-3:26257
      - --locality=region=aws,zone=z2
      - --cache=64MiB
      - --max-sql-memory=64MiB
    ports:
      - "0.0.0.0:26258:26257"
      - "127.0.0.1:8092:8080"
    volumes:
      - crdb2-data:/cockroach/cockroach-data
    networks: [crdb-net]
    healthcheck:
      test: ["CMD-SHELL", "cockroach sql --insecure --host=localhost:26257 -e 'SELECT 1' >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  crdb-3:
    image: cockroachdb/cockroach:v23.2.4
    container_name: tiendatech-crdb-3
    restart: unless-stopped
    command:
      - start
      - --insecure
      - --advertise-addr=crdb-3:26257
      - --listen-addr=0.0.0.0:26257
      - --http-addr=0.0.0.0:8080
      - --join=crdb-1:26257,crdb-2:26257,crdb-3:26257
      - --locality=region=aws,zone=z3
      - --cache=64MiB
      - --max-sql-memory=64MiB
    ports:
      - "0.0.0.0:26259:26257"
      - "127.0.0.1:8093:8080"
    volumes:
      - crdb3-data:/cockroach/cockroach-data
    networks: [crdb-net]
    healthcheck:
      test: ["CMD-SHELL", "cockroach sql --insecure --host=localhost:26257 -e 'SELECT 1' >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  crdb-init:
    image: cockroachdb/cockroach:v23.2.4
    container_name: tiendatech-crdb-init
    depends_on:
      crdb-1:
        condition: service_started
      crdb-2:
        condition: service_started
      crdb-3:
        condition: service_started
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >
        until cockroach sql --insecure --host=crdb-1:26257 -e "SELECT 1" >/dev/null 2>&1; do
          cockroach init --insecure --host=crdb-1:26257 >/dev/null 2>&1 || true;
          sleep 2;
        done;
        cockroach sql --insecure --host=crdb-1:26257 --file=/docs/db/schema.sql
    volumes:
      - ./docs/db/schema.sql:/docs/db/schema.sql:ro
    networks: [crdb-net]
    restart: "no"

volumes:
  crdb1-data:
  crdb2-data:
  crdb3-data:

networks:
  crdb-net:
    driver: bridge
